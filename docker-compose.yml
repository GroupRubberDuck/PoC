version: "3.9"

services:
  app:
    build: .
    container_name: myapp
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: python -m myapp.main